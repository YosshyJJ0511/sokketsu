class SokketsuController < ApplicationController
  #候補日の入力から開始
  def index
  end
  def update
  end 
  def create
    #（保留）@sokketsu = Sokketsu.create(text: comment_params[:text], tweet_id: comment_params[:tweet_id], user_id: current_user.id)
    #（保留）redirect_to "/tweets/#{@comment.tweet.id}"   
  end
end
